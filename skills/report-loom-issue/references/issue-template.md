## What I observed

<!-- The symptom, concretely: what Loom did and what you expected instead.
     No theory yet. -->

## Evidence

<!-- REQUIRED. Only things you actually saw: repo-relative file:line, the
     tool call or command you ran and what came back (paraphrased if it came
     from non-loom work), a failing check. If your only evidence is "the code
     reads like it does X", say exactly that and cite the lines. If you
     reproduced it, the exact steps. -->

## Impact if real

<!-- Who or what breaks, and how badly. "Dependents never release and the
     workstream stalls" and "a label reads stale for one render" are very
     different triage outcomes. -->

## Not verified

<!-- REQUIRED. What you did NOT check. Be specific and generous — this bounds
     the claim and stops a human chasing a phantom. e.g. "did not check whether
     the parent had already set the lane", "did not read the reactor that
     consumes this receipt", "did not try on a fresh thread". -->

## Alternative explanations

<!-- REQUIRED. How could this be correct behaviour that you misread? List at
     least one. If you genuinely cannot think of one, say why the behaviour
     cannot be intentional. -->

## Where I hit it

<!-- In Loom terms only: which tool, UI action or role, at what point in a
     thread's life ("a gated reviewer's second rework round", "a coder child's
     first turn after spawn", "a non-loom project's shipper thread").
     MAY say: tool names, repo-relative Loom paths, your role, that it was a
     non-loom project.
     MUST NOT say: the thread title, project, client, branch, worktree path,
     people, hosts. The footer already carries your thread id and model. -->
