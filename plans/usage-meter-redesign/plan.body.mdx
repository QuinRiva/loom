# Usage meter — designed from the hierarchy

<Callout tone="decision">
  **What you are deciding.** The shape of the sidebar usage meter, before any code. The
  argument is the table below: each decision the meter serves, the smallest signal that
  answers it, and where that signal lands. Three approaches then differ in *how* they
  communicate the same signals — not in which windows they show. Each has a `ReviewChoice`;
  the three calls the intent brief leaves open are in the `QuestionForm` at the bottom.
  **Silence defaults to the recommendation (Timeline).** A standalone real-size mockup of it
  is at `.artifacts/usage-meter-mockup-v2-20260926/index.html`.
</Callout>

## The hierarchy

Every entry traces to `intent.md`. The footer is 248 px wide; tier 1 must be read in its
first ~40 px, tier 2 must be compact all week and legible in the last 36 hours, tier 3
does not belong in the footer at all.

<Table
  density="compact"
  columns={["Decision (tier)", "Minimum signal that answers it", "Where it lands"]}
  rows={[
    ["1 · Will I have to stop? (T1)", "Pool 5-hour: the projected time-to-empty at the current burn, compared with the reset and the end of the working day. A percentage cannot say this; a time can.", "Footer line 1 (text) and the pool bar's ▼ marker; the header chip"],
    ["2 · Discretionary task now, or wait? (T1)", "Headroom against pace: how far the pool fill sits behind the now-line.", "The same pool bar — no extra element"],
    ["3 · Fable or Opus for this orchestrator? (T1)", "Pooled Fable-weekly fill against the week's elapsed share: a tick on a bar, never words.", "Footer line 3; the header chip"],
    ["4 · Each sub's 5-hour position and reset (T2)", "Five bars whose ends are their resets and whose fill is their spend, plus a countdown.", "Five 11 px rows"],
    ["5 · Weekly / Fable exceptions per sub (T2)", "One two-sided mark — ▲ risk (near max, or ≥20 pts over pace) or ▽ opportunity (≤50% used, resets within 12 h) — silent otherwise; the reason on hover.", "A glyph before the row's countdown; the popover"],
    ["6 · Two reading modes across the week (T2)", "Marked rows take colour and weight; unmarked rows stay muted. Nothing moves and nothing grows.", "The same rows"],
    ["7 · Which sub a thread is drawing from (T3)", "\u201cLast served by carl3@ · 4 m ago\u201d, an observation, never a binding. A live-thread count per sub.", "The thread header; a line in the sub popover — slice 2, task c61fa90a"],
    ["8 · Overall weekly (T3)", "Covered by the ▲ rule; no permanent column.", "Glyph and hover only"],
    ["9 · Override (T3)", "The model picker's direct anthropic-<tag> provider; loom cannot move a thread inside the pool.", "A link in the popover footer"],
  ]}
/>

Deliberately **not** in the footer: any weekly percentage, any weekly reset, the Fable
carve-out per sub, pace as a word, Codex (unless its weekly is ≥80% — it is 100% today, so
it shows as one red exception line), and any account not in the pool.

@@IN_PLACE@@

## Approach A — Timeline (recommended)

One wall-clock axis for everything in the 5-hour tier: the pool's window and each sub's.
The bar *is* the window — its right end is the reset, its fill the spend. A single
vertical now-line runs through all six bars: **fill past the line is over pace**, and the
gap between the line and a bar's end is its time to reset, so the staggered 5-hour clocks
read as geometry rather than as five countdowns. A ▼ marks where the current burn empties
a window when that lands before its reset — **amber** if it also lands before the end of
the working day, **hollow grey** if the day ends first. The dashed line is the end of the
working day when it falls inside the axis. The Fable line is a plain pace bar on the week.

Heights are fixed: **111 px in every state, 128 px while Codex's weekly is in
exception**. Nothing reflows as thresholds cross.

@@A_STATES@@

The pair the brief asks the meter to distinguish. Same used percentage on the left as at
10 am in the danger state above; the geometry says why one is a stop and the other is not.

@@A_PAIR@@

Weekly exceptions in the last 36 hours: jacob@ (91%, resets in 4 h) and carl4@ (88%,
resets in 1.5 days) carry ▲; caaarl@ (35%, resets in 9 h) carries ▽ — spend there. In the
real mid-week reading only carl4@'s Fable clock (77% at 47% of the week) earns a mark.

<ReviewChoice itemId="approach-a" label="Approach A — Timeline (recommended)" placeholder="Reject → name B or C, or say what the axis fails to tell you" />

### Hover and the header chip

<Columns>
  <Column label="Row hovered">@@HOVER_ROW@@</Column>
  <Column label="Popover">@@POPOVER@@</Column>
</Columns>

@@CHIP@@

## Approach B — Budget as hours

The headline is a **figure**, not a bar: hours of work left in the pool at the current
burn, beside the reset. Bars shrink to 28 px pace glyphs, and the five subs become a strip
of columns with their marks above — shape only; their resets and projections live on
hover. **~83 px.** The cheapest in height and the most numeric; it loses the per-sub reset
stagger and the end-of-day comparison, and after a reset or a reload the figure reads "—"
for ten minutes.

@@B_STATES@@

<ReviewChoice itemId="approach-b" label="Approach B — Budget as hours" placeholder="Accept only if the hours figure matters more than seeing the five clocks" />

## Approach C — Weighted rows

Normalised 0–100 bars with the pace tick, the convention the Limits page already uses, and
rows that earn their height: a **quiet** sub is a 6 px line, a sub that diverges ≥15 pts
from the pool, resets within 30 min or will empty before its reset is **raised** to a
12 px row with its numbers, and a sub with a weekly mark is **loud** at 21 px with the
weekly reason under it. **97–133 px depending on the state.** The most attention-shaped
of the three; the cost is a footer that changes height as thresholds cross, and five bars
that cannot be compared for reset order without reading five countdowns.

@@C_STATES@@

<ReviewChoice itemId="approach-c" label="Approach C — Weighted rows" placeholder="Accept only if adaptive height beats a fixed footer for you" />

## Recommendation

Timeline. It is the only approach where the tier 1 question is answered by *position*
(where the ▼ lands relative to the bar end and the dashed line) rather than by reading
two numbers, and the only one where the five 5-hour clocks are comparable at a glance
without five countdowns — while staying fixed-height at 111 px against the rejected 150–200 px. What would change it: if the 4 px bars are unreadable at real size on your
display, or the ▼ and dashed markers do not register without the legend, Weighted rows is
the fallback (same data derivation; only the bar geometry differs).

## Data

**Time-to-empty needs a burn rate the wire does not carry.** Each account window arrives
as `usedPercent`, `resetsAt`, `windowDurationMins` every 5 minutes via
`subscribeServerConfig` (`UsageLimitSourceSnapshot`, `apps/server/src/usage/UsageLimitSources.ts`).
The rate is derived **client-side** in `apps/web/src/loom/subscriptionMeter.ts`:

- A module-level ring keyed `${account.key}:${window.id}` keeps the last **6 readings**
  (30 min) of `(checkedAt, usedPercent)`, appending only when `checkedAt` changes; it is
  cleared when `resetsAt` moves or `usedPercent` falls (a new window).
- Rate = (last − first) ÷ elapsed once **≥3 readings span ≥10 min**. Until then the
  fallback is the window-average `usedPercent ÷ elapsed hours` — exactly what the pace tick
  already shows — and the ▼ is drawn hollow to say the projection is coarse.
- Pool rate and pool fill are the means over the five subs (`poolWindows` already averages
  this way). Right after a 5-hour reset the ring empties and the window-average is ~0, so
  no marker is drawn for ten minutes; after a page reload the same ten minutes apply.
- Pace ticks use `elapsedShare()` from `packages/shared/src/usageLimits.ts`; the pool's
  tick is the mean elapsed share.

Client-side because `UsageLimitSources.ts` and `packages/contracts/src/providerUsageLimits.ts`
are upstream files (t3code #9875, #10395) that upstream is still changing; a rate field
there is a marked hunk on a contract that would conflict at every pull. If the ten-minute
blind spot after reloads proves annoying, or mobile wants the meter, the same ring moves
into a `usageBurn.loom.ts` beside the poller and publishes one `// loom:` field — about
40 lines relocated.

**End of working day** is a constant, 18:00 local, in `subscriptionMeter.ts` (question 1).

**Slice 2 — last served by, live-thread counts** (task c61fa90a, researcher report
`322ce596`): the hub names the account on every response in `X-Cpa-Trace-Id`; a
loom-injected pi extension forwards it and the server maps index → email. That gives the
thread header a "carl3@ · 4 m ago" chip and the sub popover a "3 live threads" line, both
drawn greyed in the popover above. Nothing in the footer waits on it.

<FileTree
  title="Implementation sketch (after acceptance) — ~200 lines web, no server or contract change"
  entries={[
    { path: "apps/web/src/loom/subscriptionMeter.ts", change: "modified", note: "derive the shared axis, per-sub rows, projections (ring + fallback), weekly marks, pool means; the reading ring" },
    { path: "apps/web/src/loom/SubscriptionMeter.tsx", change: "modified", note: "SVG timebar/pacebar; rows replace the per-driver bars; popover per sub; chip carries tier 1 only" },
    { path: "apps/web/src/loom/subscriptionMeter.test.ts", change: "modified", note: "projection, marker rule (reset vs end of day), mark thresholds, ring reset on the three fixture states" },
    { path: "apps/web/src/components/usage/UsageLimitsPooled.tsx", change: "modified", note: "export accountHue so meter and Limits colour a sub identically" },
    { path: "apps/web/src/preview/fixtures.tsx", change: "modified", note: "the three states as preview-harness fixtures for the /preview route" },
  ]}
/>

## Verification

Preview harness (`docs/web-component-preview.md`) with the three fixture states: the
now-line is one continuous vertical across pool and rows, the ▼ sits before the bar end
only in the danger state and is hollow at 16:30, the Fable tick sits right of the fill in
the last-36-hours state, and the footer measures ≤ 128 px in every state. Then the
dev-verify recipe on a `VACUUM INTO` copy of the cockpit DB with the hub registered (its
management key is wrong today and must be fixed first): five rows match Usage → Limits for
the same accounts; collapse the sidebar and the chip shows the 5-hour bar and Fable tick;
hover a row and the popover lists 5-hour, weekly and Fable with resets. `vp check` and
`vp run typecheck` green.

<QuestionForm
  questions={[
    {
      id: "eod",
      title: "Where does the end of the working day come from?",
      subtitle: "The dashed line and the amber-versus-hollow rule for the empty marker both depend on it.",
      refs: [{ label: "Approach A — Timeline (recommended)", anchor: "approach-a-timeline-recommended" }],
      mode: "single",
      options: [
        { id: "const", label: "A fixed 18:00 local, as a constant in the meter code", recommended: true, detail: "One line; change it in code if the day moves." },
        { id: "setting", label: "A setting in settings.json (usageMeter.workingDayEnd)", detail: "Editable without a deploy; costs a settings key and a schema line." },
        { id: "none", label: "No end-of-day at all", detail: "Every projected empty before a reset is amber; 16:30 reads the same as 10:00." },
      ],
    },
    {
      id: "marks",
      title: "Thresholds for the ▲ risk and ▽ opportunity marks",
      subtitle: "Risk fires when a weekly or Fable-weekly clock is near max or far enough over pace to hit max before it resets; opportunity when plenty is left and the reset is close.",
      refs: [{ label: "The hierarchy", anchor: "the-hierarchy" }],
      mode: "single",
      options: [
        { id: "drawn", label: "As drawn: risk at ≥85% used or ≥20 pts over pace; opportunity at ≤50% used and resets within 12 h", recommended: true, detail: "One mark in the real mid-week reading (carl4@ Fable), three in the last-36-hours state." },
        { id: "max-only", label: "Risk only at ≥85% used; no pace test", detail: "Quieter mid-week; a sub racing towards its weekly max is not flagged until it is nearly there." },
        { id: "wider", label: "Opportunity within 24 h rather than 12 h", detail: "More ▽ marks on the last full day; the mark becomes routine, not exceptional." },
      ],
      allowOther: true,
    },
    {
      id: "codex",
      title: "Does Codex share the footer?",
      subtitle: "Codex is one Plus account read from the pi instance, not the hub; its weekly is 100% today and resets in 2 days.",
      mode: "single",
      options: [
        { id: "exception", label: "One line only while its weekly is ≥80% (as drawn, red at 100%)", recommended: true, detail: "Silent the rest of the time; the Limits page keeps the full picture." },
        { id: "always", label: "Always a line under the pool", detail: "+12 px in every state." },
        { id: "never", label: "Never in the footer", detail: "Limits page only; the header chip stays Claude-only too." },
      ],
    },
  ]}
/>
