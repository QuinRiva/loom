---
manager_sessions:
  - id: 066af3a5-e6b0-446b-b078-e96b1b391b55
    role: plan
    authored_at: 2026-08-04T05:07:34.501Z
---

# Sidebar v2 re-home: Pi navigation on upstream's inbox

**Objective.** Move loom's Pi navigation onto upstream's Sidebar v2 (the flat thread inbox with settled/snoozed shelves) so future upstream sidebar work merges as cheap unions, while Pi's orchestration affordances stay first-class. **Done** means: the design below is agreed, loom users on v2 see workstream roll-ups and attention state, goal management has a home that does not depend on the retired thread-nesting, upstream's settled lifecycle behaves coherently for workstream threads, and everything deliberately un-adopted is recorded for the next cadence pull.

**Decision context.** Three investigations and an option-space study ground this plan. The headline evidence: across the 147 upstream commits since our merge-base, v2's data contracts (row props, list partition, the `latestTurnDiff` stub) did not change by a byte while ~75% of its render JSX was rewritten — so loom attaches at the frozen seams and never builds inside the render tree. Upstream has already defaulted v2 on for its nightly/dev builds and has an unmerged branch deleting v1 outright. Loom's 35 orchestration fields (`goalId`, `planLane`, `attention`, …) are already on the shell type v2 renders, so no new data plumbing is needed for any option.

<Callout tone="decision">
  The direction set by the human and confirmed by the evidence: adopt upstream v2 wholesale; drop goal-nested thread lists from the sidebar; keep durable goals; re-integrate the few loom features that matter. Goals are never navigated to directly — a goal is reached through a thread that carries it, and exists to track a larger piece of work (its tasks, and the handoff chain when work outgrows one thread's context). Browsable/standalone goal surfaces are rejected. A clickable mock of an earlier draft is at `.artifacts/sidebar-v2-goal-panel-mock/index.html`; the built design differs in one respect — the goal-switcher dropdown in the panel header is removed (the panel is always anchored to the open thread's goal).
</Callout>

## What the user sees

The sidebar becomes upstream's flat inbox — active cards, snoozed and settled shelves — with two loom touches on rows: a workstream roll-up badge on roots that have agent children, and attention-aware status. The goal lives in the right panel:

<Screen
  surface="panel"
  caption="The Goal panel — GoalTasksPanel promoted into the single goal surface, always anchored to the open thread's goal"
  html={'<div class="wf-card"><div style="display:flex;align-items:center;gap:8px"><strong>Sidebar v2 re-home</strong><span class="wf-pill">goal</span><span style="margin-left:auto" class="wf-muted">⋯</span></div><small class="wf-muted">Decide and build how loom&#39;s sidebar lives on upstream&#39;s Sidebar v2.</small><hr/><small><strong>Tasks</strong> · 4/6</small><p>☑ Wave 1 — investigation<br/>☑ Wave 2 — option space<br/>☐ Wave 4 — design decision</p><hr/><small><strong>Threads</strong> · handoff order</small><div class="wf-box"><span class="wf-chip">staged</span> Implementation phase</div><div class="wf-box" style="border-color:var(--wf-accent)"><span class="wf-chip accent">current</span> Sidebar v2 re-home for Pi</div><div class="wf-box wf-muted"><span class="wf-chip">settled</span> Design exploration (predecessor)</div><button>+ New session under this goal</button></div>'}
/>

- The **entry point** is a goal chip on the thread's chat header (`◎ goal title · N threads`), beside the existing `ThreadLineageBreadcrumb`; goal-less threads show no chip; clicking toggles the panel. The panel is **always the open thread's goal** — there is no goal browsing, no goal switcher, and no standalone goal surface anywhere.
- The **Threads section** lists the goal's threads in serial handoff order; settled predecessors are dimmed but navigable (settled is not archived — visiting one does not unsettle it). This is what replaces sidebar nesting: the chain is visible from any thread on the goal.
- **Goal creation stays thread-first**: "Create goal from thread" and "Assign to goal" live on the v2 row context menu (a structured dialog replaces the current three `window.prompt`s). Rename/archive/delete and "New session under this goal" live in the panel's overflow menu. A goal whose threads are all archived becomes unreachable from the UI — accepted: archiving the last thread means the work is finished with.

## Common base

1. **Quick wins, already in flight behind the existing `sidebarV2Enabled` flag** (coder + review gate running): root-thread filter in v2's partition (children stop appearing as top-level inbox rows), workstream roll-up badge on v2 rows, `attention` honoured in `resolveSidebarV2Status` (a `needs_guidance` thread must not read as "ready"), `latestTurnDiff` filled from `diffAdditions`/`diffDeletions`, and `SidebarAccountUsagePill` composed into upstream's `SidebarChrome.tsx`. Defaults unchanged.
2. **Settle semantics — blockers and triggers, one direction only.** Workstream state feeds the settle classifier in `packages/client-runtime/src/state/threadSettled.ts` but never becomes a derived column and never writes `settledOverride`:
   - Three never-settle blockers: any `attention` flag; `planLane` is `yielded`; any non-terminal descendant (the idle parent orchestrating running children must not settle).
   - One trigger (no-override case): lane `done`/`cancelled` with fan-in settled and no `pendingRework` settles immediately instead of waiting out the inactivity window.
   - `planned` threads: neither trigger nor blocker. Group/goal quiescence reuses `rollupGraphState` precedence rather than inventing a second rule.
3. **Handoff chain becomes data.** Today the `goal_continue` predecessor pointer is English prose inside the brief text — sibling order is not defensibly queryable. One nullable column fixes it, mirroring the existing `fork_from_thread_id` precedent; the same change persists the currently-discarded `handoff-recorded` destination ids (today reduced to a counter):

<DataModel
  entities={[
    {
      id: "pt",
      name: "projection_threads",
      fields: [
        { name: "goal_id", type: "TEXT", nullable: true, note: "exists — Migration 1003" },
        { name: "fork_from_thread_id", type: "TEXT", nullable: true, note: "exists — Migration 1023 (the precedent)" },
        { name: "continues_thread_id", type: "TEXT", nullable: true, fk: "projection_threads.id", change: "added", note: "written by goal_continue; surfaced on the shell" },
      ],
    },
  ]}
/>

4. **Deletions once v2 is loom's default:** the nesting renderer (`SidebarGoalThreadList.tsx`), the goal-collapse store (`sidebarUiStore.ts` and its localStorage key), the jump-map alignment machinery (~120 lines of `Sidebar.logic.loom.ts` — keeping `resolveGoalWorktreeSeed` and `isStagedHandoffThread`), and loom's inline chrome fork in `Sidebar.tsx`. The goal-worktree seed keeps its top precedence untouched — it is what makes a handoff successor land in its predecessor's worktree.
5. **Rejected direction, recorded so nobody re-proposes it:** goal grouping or tree drill-ins *inside* the v2 list. It rebuilds the nesting the human dropped, in the one region upstream rewrote 75% of in nine days.

## Alternatives considered and why they lost

The design study produced three candidates. **A (thread-anchored chip + popover)**: goal affordances only in a popover off the chip — rejected as the end-state because cramming the task tree, chain, and CRUD into a popover duplicates what the existing `GoalTasksPanel` right-panel already hosts; the panel is the same idea with a better home (the chip remains, as the panel's entry point). **B-with-switcher (goal panel as a browsable goals list)**: a goal-switcher dropdown doubling as a per-project goals list, plus ex-nihilo "New goal…" — rejected by the human on the model above: you never select a goal by itself; a goal has no standalone interactability, so a goals list is navigation to nowhere, and it introduced a panel-goal-versus-open-thread divergence state for no benefit. **C (goals in sidebar nav)**: a browsable goals pane in the sidebar chrome — rejected for the same reason, plus the most new scope, and loom already retired a thread-less goal landing as a dead end once (`_chat.index.tsx` records why). What remains is the thread-anchored goal panel: A's anchoring with B's surface.

## Implementation shape

<FileTree
  title="Change map — B"
  entries={[
    { path: "apps/server/src/persistence/Migrations/1035_ThreadContinuesFrom.ts", change: "added", note: "continues_thread_id (+ handoff destination ids); next free loom number at build time" },
    { path: "apps/server/src/mcp/GoalHandoffHttp.ts", change: "modified", note: "goal_continue stamps continues_thread_id" },
    { path: "packages/contracts/src/orchestration.loom.ts", change: "modified", note: "continuesThreadId on thread + shell" },
    { path: "packages/client-runtime/src/state/threadSettled.ts", change: "modified", note: "loom-marked additive: 3 blockers + 1 trigger" },
    { path: "apps/web/src/loom/GoalChip.tsx", change: "added", note: "chat-header entry point, beside ThreadLineageBreadcrumb" },
    { path: "apps/web/src/components/GoalTasksPanel.tsx", change: "modified", note: "promote: Threads section, overflow CRUD (rename/archive/delete, new session)" },
    { path: "apps/web/src/loom/goalThreadChain.ts", change: "added", note: "pure: topological handoff-order walk over shells; unit-tested" },
    { path: "apps/web/src/loom/sidebarGoalActions.ts", change: "modified", note: "reuse CRUD actions; rehost create/assign on v2 row context menu" },
    { path: "apps/web/src/loom/SidebarGoalThreadList.tsx", change: "removed", note: "nesting renderer — deleted at default flip" },
    { path: "apps/web/src/loom/sidebarUiStore.ts", change: "removed", note: "goal-collapse store — deleted at default flip" },
    { path: "docs/upstream-sync/23-sidebar-v2-rehome.md", change: "added", note: "decision record + deliberately-un-adopted list for cadence pulls" },
  ]}
/>

Ordered milestones, each independently landable behind the flag:

1. **Quick wins** (in flight) — v2 dogfoodable in loom.
2. **Chain data** — migration + `goal_continue` write + shell field. Nothing user-visible.
3. **Chip + panel Threads section** — the chain becomes visible from any thread on the goal.
4. **Panel promotion** — overflow CRUD in the panel; rehost create/assign on the v2 row context menu with a structured dialog replacing the three `window.prompt`s.
5. **Settle semantics** — blockers + triggers in `threadSettled.ts`, with cases added to its existing test file.
6. **Default flip + deletions** — v2 becomes loom's default surface (v1 stays mounted on `/settings` until upstream's deletion arrives); nesting machinery deleted; decision record written.

**Non-goals:** `WorkstreamPanel`/`WorkstreamGraph` (untouched — ChatView-side); restoring known intentional fork drops; task editing in the panel (tasks stay agent-written); any goal grouping inside the v2 list; deploying.

## Verification

Per milestone: `vp run typecheck`, `vp check`, targeted suites (`Sidebar.logic.test.ts`, `workstreamRollup.test.ts`, `threadSettled.test.ts`, new `goalThreadChain` tests). Milestone 2 additionally gets the DB-copy migration smoke against a copy of the cockpit `state.sqlite` on a spare port. End-to-end: the dev-verify recipe with a seeded workstream — enable v2, confirm children hidden, roll-up badge + popover navigation, attention states, chip → panel → navigate a settled predecessor without unsettling it, create a goal from a thread via the row context menu, new-session-under-goal lands in the goal's worktree. Upstream v2 has no component tests, so live verification is the bar, not an optional extra.

## Risks

- **Cadence-pull collisions** are the designed-against risk: every attachment lands at seams that were byte-identical across 147 commits, in loom-owned files, or as loom-marked additive hunks. The one accepted exposure is `threadSettled.ts` (three upstream commits in the window) — additive, marked blocks minimise re-apply cost.
- **Upstream's dev default flip** (`c13a021e4`) arrives at the next pull and would flip loom dev builds to v2 while the deployed cockpit stays v1 — question 2 below decides the posture *before* it surprises us.

<QuestionForm
  questions={[
    {
      id: "quickwins",
      title: "Quick wins started pre-approval (behind the off-by-default flag) — confirm?",
      mode: "single",
      options: [
        { id: "yes", label: "Confirmed — land them once the review gate passes", recommended: true },
        { id: "hold", label: "Hold the branch until the whole design is approved" },
      ],
    },
    {
      id: "stage",
      title: "When upstream's channel-derived default arrives, loom dev builds flip to v2 while deployed cockpit stays v1. Posture?",
      mode: "single",
      options: [
        { id: "accept", label: "Accept the split as dogfooding", recommended: true, detail: "Dev on v2 early; deployed flips only at milestone 6." },
        { id: "pin", label: "Pin loom to v1 everywhere until milestone 6", detail: "One loom-marked hunk in branding.logic.ts; uniform but no early dogfood." },
      ],
    },
    {
      id: "chain",
      title: "Land the handoff-chain schema (continues_thread_id + persist handoff destination ids)?",
      mode: "single",
      options: [
        { id: "yes", label: "Yes — milestone 2 as planned", recommended: true, detail: "Every option's ordering claim depends on it; precedented; also fixes the staged-successor ambiguity." },
        { id: "no", label: "No — show goal siblings by createdAt only", detail: "Order is wrong whenever a goal has parallel roots or forks." },
      ],
    },
  ]}
/>
