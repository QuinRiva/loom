# Cadence pull 7 — remaining typecheck errors after session 7

Measured with `vp run typecheck` (all 15 packages) at session 7's last commit,
AFTER `vp i`.

    GREEN (13 of 15): contracts, shared, client-runtime, effect-acp,
                      effect-codex-app-server, oxlint-plugin-t3code, ssh,
                      tailscale, scripts, marketing, apps/desktop,
                      **apps/web (117 -> 0)**, + 1 other

    apps/server   26   (was 176 at the start of session 7)
    apps/mobile   63   (untouched this session)

## apps/web — 0. DONE.

Session 7 took it 117 -> 0. Nothing is stubbed or suppressed; every fix was
either a restored loom declaration, a corrected caller, or a fixture updated to
a shape a parent genuinely changed. Two deliberate deletions are recorded in
§Session 7 of the sync note (the unadopted ChatMarkdown workspace-image test and
two orphaned `pendingUserInput` composer-logic cases).

## apps/server — 26 (every one in a test/integration file)

     4 src/orchestration/Layers/ProviderCommandReactor.test.ts
     3 src/orchestration/Layers/OrchestrationEngine.test.ts
     2 src/provider/makeManagedServerProvider.test.ts        (maintenanceCapabilities)
     2 src/project/AgentSessionImporter.test.ts              (missing layer: ProviderLaunchClaims | WorkspaceOccupancy)
     2 src/orchestration/Layers/ProviderRuntimeIngestion.test.ts (user-input `answers` payload shape)
     2 src/orchestration/Layers/ProjectionSnapshotQuery.test.ts  (missing ServerConfig in layer)
     2 integration/orphanedProviderSessionStartup.integration.test.ts (missing ExhaustionResumeSweep)
     2 integration/OrchestrationEngineHarness.integration.ts (missing PullRequestSyncReactor in layer)
     1 each: ProviderSessionReaper, ClaudeAdapter (requestId), AntigravityProtocol,
            AgentSessionScanner, ThreadSettlementReactor, projector (Effect-aware
            test API), HandoffDrafterReactor

The bulk classes session 7 already cleared, for reference when they recur:
  - whole-shape service doubles missing a member one parent added:
    `removeIfStopped` (loom's), `readStreamEvents` / `readThreadEvents` /
    `getThreadReplayStats` / `getSession` (upstream's). Fixed either by adding
    the member or, better, by switching `Effect.provideService(Tag, {...})` to
    `Effect.provide(Layer.mock(Tag)({...}))` — `Layer.mock` fills the rest with
    unimplemented defects, so the NEXT pull's new members cannot break the test.
  - thread fixtures missing the ~27 loom workstream fields. There is now ONE
    shared block for this: `apps/server/src/orchestration/deciderTestThread.ts`
    exports `loomThreadFixtureDefaults` (read-model row) and
    `loomThreadShellFixtureDefaults` (shell row). Spread it first, override after.
  - `defaultStartFromOrigin` (new required project field), `goals: []` (new
    required read-model/snapshot field), `emitsExitOnStop` (new REQUIRED
    provider capability — codex's real adapter says `true`).
  - `{ ...base, ...overrides }` where `overrides: Partial<T>`: under
    `exactOptionalPropertyTypes` that widens every field back to optional. Use
    `Object.assign(base, overrides)`.

## apps/mobile — 63. NOT STARTED.

    40 src/features/threads/PendingUserInputCard.tsx
    18 src/state/use-selected-thread-requests.ts
     1 each: pending-thread-creation.ts (needs loomThreadShellFixtureDefaults'
            field set), threadActivity.test, ThreadDetailScreen,
            QuestionAnswerHistory.test, environment-cache-store.test

Session 4's guidance stands, and the brief's is more specific:
`use-selected-thread-requests.ts` needs the upstream-only declarations restored
(`Atom` import, `scopedRequestKey`, `selectedThreadKey`, `dismissingUserInputId`)
and then loom's `@t3tools/shared/userInputAnswers` hoist re-applied on top.
`PendingUserInputCard.tsx` compiles as upstream's redesign (loom's option
previews and multi-select are tracked separately as task `a4b1ddfa`); note its
props are consumed by `ThreadDetailScreen.tsx:1023`, which still passes loom's
`maxHeight` / `collapsed` / `onToggleCollapsed` / `onStopThread`, so caller and
callee must be decided together.

## Method that worked (reuse it)

1. `docs/upstream-sync/pull7-tools/whatismissing.sh <repo-relative-path> <name>...`
   on every TS2304/TS2305/TS2552/TS2724 — it names which parent still declares
   the symbol. Note: the path must be relative to the REPO ROOT, and the tool
   must be run from the repo root.
2. Prop/member-shape errors (TS2739/TS2740/TS2741) are almost always "loom's
   caller, upstream's callee" (or the reverse). Diff the callee against BOTH
   parents before touching the caller.
3. **Delete the stale `apps/<app>/tsconfig.tsbuildinfo` before believing a
   count.** `composite: true` means tsc reports cached errors against paths that
   no longer exist; session 7 lost time chasing a `@types/hast` mismatch that
   the cache alone was reporting.
